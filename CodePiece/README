/*
 * This directory saves a set of code pieces.
 */
pthread_cond.c --> A good example from memcached implementation about pthread
                   conditional variable.
struct_pointer.c --> A simple example on struct collocation.
linklist_reverse.c --> A linklist reverse.
